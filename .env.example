# ============================================================
# Java Challenge System — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values for local dev.
# For Railway deployment, set these directly in the Railway dashboard.

# --- GitHub OAuth (required) ---
# Create at: https://github.com/settings/developers → New OAuth App
# Homepage URL:     https://your-frontend.railway.app
# Callback URL:     https://your-backend.railway.app/login/oauth2/code/github
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# --- OpenAI (required for AI variant generation) ---
# Get at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_key_here

# --- NextAuth (frontend) ---
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=change-me-to-random-32-char-string
NEXTAUTH_URL=http://localhost:3000

# --- Database (auto-set by Railway Postgres plugin) ---
# Local dev only — Railway provides DATABASE_URL automatically
DATABASE_URL=jdbc:postgresql://localhost:5432/javachallenge
DB_USER=postgres
DB_PASSWORD=postgres

# --- Service URLs (auto-set in docker-compose; override for Railway) ---
FRONTEND_URL=http://localhost:3000
GRADER_URL=http://localhost:8081
NEXT_PUBLIC_API_URL=http://localhost:8080
BACKEND_URL=http://localhost:8080
